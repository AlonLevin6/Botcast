<div dir="rtl">

# **Daily Commute Podcast Bot \- Project Plan**

## **1\. מוטיבציה (Motivation)**

ניצול זמן הנסיעה (בעיקר על דו-גלגלי) להאזנה לתוכן מותאם אישית.

במקום להסתמך על פלייליסטים או רדיו, המטרה היא לייצר פודקאסט יומי של כ-15 דקות בנושאים נבחרים.

הדגש הוא על מינימום חיכוך (אפס התעסקות עם מסכים בזמן נסיעה), פעולה חלקה, וזמינות מהירה (המערכת מקבלת נושא, ותוך מספר דקות האודיו מוכן להאזנה). נדרשת יכולת לתזמן מראש או לבקש יצירה "מעכשיו לעוד חמש דקות".

## **2\. הגדרת ה-MVP (Minimum Viable Product)**

בוט אישי המאפשר שליחת נושא בטקסט, ומחזיר קובץ אודיו (MP3) המכיל פודקאסט מוקרא באנגלית על הנושא המבוקש, באורך שמתאים לכ-15 דקות האזנה. ה-MVP מתוכנן לעבוד במודל של 0 עלויות שוטפות, ולהוות תשתית (Walking Skeleton) שניתן יהיה להרחיב בעתיד (למשל, חיבור ל-RSS או הרצה על שרת ביתי מסוג CasaOS).

## **3\. ארכיטקטורה וטכנולוגיות (Tech Stack)**

* **שפת פיתוח:** Python.  
* **ממשק קלט/פלט:** Telegram Bot API (חינמי, מהיר, ונגיש בקלות ללא אישורים מיוחדים, תומך בשליחת קבצי שמע גדולים).  
* **מודל שפה ליצירת תסריט (LLM):** Google Gemini 1.5 Flash/Pro (באמצעות Google AI Studio \- שכבה חינמית).  
* **המרה לקול (TTS):** edge-tts (ספריית קוד פתוח בפייתון המשתמשת במנוע של Microsoft Edge. חינמית, ללא צורך במפתח API, ובעלת קולות טבעיים באנגלית).  
* **סביבת הרצה (Hosting/Infrastructure):** בשלב הראשון הרצה מקומית, ולאחר מכן פריסה על Google Cloud Platform (GCP) באמצעות מכונת e2-micro תחת מסלול "Always Free".

## **4\. ארכיטקטורת זרימת נתונים (Data Flow)**

1. המשתמש שולח הודעת טקסט לבוט בטלגרם עם הנושא הרצוי.  
2. סקריפט ה-Python מקבל את הבקשה ומעביר פרומפט מערכת מובנה (הוראות לכתיבת פודקאסט מרתק) \+ נושא המשתמש ל-Gemini API.  
3. Gemini מחזיר תסריט טקסטואלי באורך מתאים (כ-2,000 מילים).  
4. התסריט מועבר לספריית edge-tts אשר מעבדת אותו ושומרת קובץ MP3 מקומי על השרת.  
5. הבוט שולח את קובץ ה-MP3 חזרה לצ'אט בטלגרם עבור המשתמש ומוחק את הקובץ המקומי מהשרת.

## **5\. אבני דרך לפיתוח (Milestones) \- למערכת הסוכנים**

* ![][image1]**הגדרות בסיס:** יצירת בוט ב-BotFather (Telegram) וקבלת Token; הפקת API Key מ-Google AI Studio.  
* ![][image1]**אבן דרך 1:** בניית שלד הבוט ב-Python (שימוש בספרייה כמו python-telegram-bot) שמאזין להודעות ומחזיר תגובת אישור (Echo/Ping).  
* ![][image1]**אבן דרך 2:** אינטגרציה עם google-generativeai. הבוט מקבל נושא, קורא ל-Gemini API עם פרומפט ייעודי לפודקאסט, ומחזיר את הטקסט הגולמי לטלגרם (לצורכי בדיקה).  
* ![][image1]**אבן דרך 3:** אינטגרציה עם edge-tts. לקיחת התסריט מ-Gemini, שמירתו כקובץ אודיו (MP3), ושליחת הקובץ לטלגרם במקום הטקסט.  
* ![][image1]**אבן דרך 4:** טיפול בשגיאות (Error handling), עבודה אסינכרונית כדי לא לתקוע את הבוט בזמן הרינדור של האודיו, והוספת אינדיקציות סטטוס למשתמש ("כותב תסריט...", "מקליט אודיו...").  
* ![][image1]**אבן דרך 5:** פריסה (Deployment) לשרת ענן כדי שהבוט ירוץ באופן עצמאי.

## **6\. רעיונות להרחבה עתידית (Backlog)**

רעיונות שעלו אך הוחלט במכוון לדחות כדי לא לסרבל את ה-MVP:

* **התאמה אישית של הפודקאסט (בהשראת Audio Overview של NotebookLM):** הוספת פרמטרים אופציונליים לפני היצירה - אורך רצוי (קצר/ברירת מחדל/ארוך) והנחיות/מיקוד חופשיים (למשל "תסביר כמו למתחילים", "תתמקד בפיתוחים האחרונים").  
* **פודקאסט דו-קריינים (Two-narrator format):** התסריט מ-Gemini יופק כדיאלוג עם תיוג דובר (למשל `HOST_A:` / `HOST_B:`), כל קטע יעבור ל-edge-tts עם קול שונה, והקטעים יחוברו לקובץ אודיו אחד. דורש עבודה נוספת על חיבור (stitching) קבצי האודיו ועל הנדסת הפרומפט כדי לקבל דיאלוג טבעי.
</div>